import calculator.JCalculator;

/**
 * Main class to launch the calculator application.
 * Creates an instance of the JCalculator class.
 *
 * @author Rémy Bleuer
 * @author Kilian Demont
 */
public class Main
{
  /**
   * The main method to start the calculator application.
   *
   * @param args Command line arguments (not used).
   */
  public static void main(String ... args) {
    new JCalculator();
  }
}

